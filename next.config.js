/**
 * @type {import('next').NextConfig}
 */
// const nextConfig = {
//     output: 'export',
//     basePath: process.env.NODE_ENV === "production" ? "/suno_scan" : undefined,
// }
// module.exports = nextConfig
